# Fluxy Backend - Production Environment Variables
# Copy this content to .env file on your VDS server

# Server Configuration
NODE_ENV=production
PORT=5000

# MongoDB Connection (Local MongoDB on VDS)
MONGODB_URI=mongodb://localhost:27017/fluxy

# JWT Secret (Generate a strong random secret!)
# You can generate one with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=YOUR_SECURE_JWT_SECRET_HERE

# Frontend URL (Your Netlify domain)
FRONTEND_URL=https://your-app.netlify.app
CORS_ORIGIN=https://your-app.netlify.app

# File Upload Configuration
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=10485760

# Session Secret
SESSION_SECRET=YOUR_SECURE_SESSION_SECRET_HERE

# API Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Logging
LOG_LEVEL=info
LOG_FILE=./logs/fluxy.log

# Optional: Email Configuration (for notifications, password reset, etc.)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# EMAIL_FROM=noreply@fluxy.com

# Optional: Redis (for session storage, caching)
# REDIS_URL=redis://localhost:6379

# Server Domain
SERVER_DOMAIN=https://api.fluxy.com
