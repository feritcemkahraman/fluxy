# Fluxy Backend - Production Environment Variables
# Copy this content to .env file on your VDS server

# Server Configuration
NODE_ENV=production
PORT=5000

# MongoDB Atlas (Cloud Database)
# Replace with your own MongoDB Atlas connection string
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/fluxy?retryWrites=true&w=majority

# JWT Secret (Generate a strong random secret!)
# You can generate one with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=YOUR_SECURE_JWT_SECRET_HERE

# Frontend URL (Netlify domain)
FRONTEND_URL=https://fluxycorn.netlify.app
CORS_ORIGIN=https://fluxycorn.netlify.app

# File Upload Configuration
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=10485760

# Session Secret
SESSION_SECRET=YOUR_SECURE_SESSION_SECRET_HERE

# API Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Logging
LOG_LEVEL=info
LOG_FILE=./logs/fluxy.log

# Optional: Email Configuration (for notifications, password reset, etc.)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# EMAIL_FROM=noreply@fluxy.com

# Optional: Redis (for session storage, caching)
# REDIS_URL=redis://localhost:6379

# Server Domain (VDS IP or domain)
SERVER_DOMAIN=http://87.121.103.236:5000
